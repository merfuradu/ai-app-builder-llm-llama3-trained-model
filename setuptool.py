from setuptools import setup, find_packages

setup(
    name='unsloth',
    version='0.1.0',
    description='A package for unsloth',
    author='Merfu',
    author_email='your.email@example.com',
    packages=find_packages(),
    install_requires=[
        'some_dependency>=1.0.0',
        # Add other dependencies here
    ],
)
